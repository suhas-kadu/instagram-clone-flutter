import 'package:flutter/material.dart';

class AddAPostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Add a post"),)
    );
  }
}